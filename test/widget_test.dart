// Tests unitarios de EDFCatalogoMultiplatform
//
// El smoke test genérico de flutter create ha sido eliminado porque la app
// no es un contador. Los tests reales se implementarán en F2-3 del plan de
// consolidación (MongoService, S3Service, AuthViewModel).
//
// Por ahora este fichero mantiene un test mínimo para que CI pase.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — tests reales pendientes en F2-3', () {
    // TODO F2-3: añadir tests unitarios para MongoService, S3Service, AuthViewModel
    expect(true, isTrue);
  });
}
